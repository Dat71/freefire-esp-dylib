name: Build BattleCats Pro Menu

on:
  push:
    branches: [ "main" ]
  workflow_dispatch: 

jobs:
  build:
    runs-on: macos-latest

    steps:
    - name: Tải mã nguồn về máy ảo
      uses: actions/checkout@v4

    - name: Thiết lập môi trường iOS SDK
      run: |
        sudo xcode-select -s /Applications/Xcode.app/Contents/Developer
        xcrun --sdk iphoneos --show-sdk-path

    - name: Biên dịch file .m sang .dylib
      run: |
        xcrun -sdk iphoneos clang -dynamiclib -arch arm64 \
        -isysroot $(xcrun --sdk iphoneos --show-sdk-path) \
        -miphoneos-version-min=14.0 \
        -framework UIKit \
        -framework Foundation \
        -framework QuartzCore \
        -framework CoreGraphics \
        BattleCats_ProMenu.m -o BattleCats_ProMenu.dylib

    - name: Xuất file thành phẩm (Artifact) để tải về
      uses: actions/upload-artifact@v4
      with:
        name: BattleCats-ProMenu-Dylib
        path: BattleCats_ProMenu.dylib
